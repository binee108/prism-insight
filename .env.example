# .env.example

# Telegram Configuration
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_AI_BOT_TOKEN=your_bot_token
TELEGRAM_CHANNEL_ID=your_channel_id

# ============================================================================
# LLM Provider Configuration
# ============================================================================
# Select which LLM provider to use. Options: 'openai', 'anthropic', 'claude-code-cli'
# Default: 'openai'
PRISM_LLM_PROVIDER=openai

# ----------------------------------------------------------------------------
# Claude Code CLI Provider Settings
# ----------------------------------------------------------------------------
# These settings are only used when PRISM_LLM_PROVIDER=claude-code-cli
#
# IMPORTANT: Claude Code CLI must be installed and authenticated on your system.
# Installation: https://docs.anthropic.com/claude/docs/claude-code
# Authentication: Run `claude auth login` before using this provider.

# Path to the claude executable (default: "claude" from PATH)
CLAUDE_CLI_PATH=claude

# Project path for -p option (optional, but recommended for context)
# This should typically be the root directory of your project
CLAUDE_CLI_PROJECT=/path/to/prism-insight

# Model name (e.g., "claude-3-5-sonnet-20241022")
# If not set, CLI will use its default model
CLAUDE_CLI_MODEL=claude-3-5-sonnet-20241022

# Additional CLI arguments
# Note: --output-format is automatically managed (always json)
# You can add other CLI options here
CLAUDE_CLI_EXTRA_ARGS=

# Timeout in seconds for CLI execution (default: 180)
# Increase this for long-running analysis tasks
CLAUDE_CLI_TIMEOUT=180

# ============================================================================
# API Keys (for OpenAI and Anthropic providers)
# ============================================================================
# These are typically required by mcp-agent framework when using API-based providers
# They are NOT needed for claude-code-cli provider

# OPENAI_API_KEY=your_openai_api_key_here
# ANTHROPIC_API_KEY=your_anthropic_api_key_here